# NeuraOps UI Environment Variables

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=NeuraOps
NEXT_PUBLIC_APP_VERSION=0.0.1

# API Configuration
# Backend API URL - change based on environment
NEXT_PUBLIC_API_URL=
# For local development: http://localhost:8000
# For production: https://your-neuraops-core-api-domain.com

# Authentication Configuration
# NeuraOps uses automatic JWT authentication via login
# No manual token configuration required
# Credentials are configured in the Core component's .env file

# Development Notes:
# - NEXT_PUBLIC_API_URL is the only required variable
# - Authentication is handled automatically via login
# - JWT tokens are managed internally by the application
# - No API keys or manual tokens needed